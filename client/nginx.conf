server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Serve static assets, fallback to index.html for React Router (if used)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy /api requests to the server container
    location /api/ {
        proxy_pass http://server:3000/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Automatically proxy root-level API requests (e.g. /channels instead of /api/channels)
    location ~ ^/(channels|auth|connections|messages|board|cards|health|stats|users|members)(/.*)?$ {
        proxy_pass http://server:3000/$1$2;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
